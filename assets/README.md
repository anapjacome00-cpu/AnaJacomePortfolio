# assets

- `logo.png` — extracted from the original inline base64 logo, used for favicon and the two nav/profile marks.
- `contact-photo.png` — the contact-section photo. The CSS crops it to a 1:1 square (`object-fit: cover`), so a wider/landscape source gets its left and right edges trimmed.
