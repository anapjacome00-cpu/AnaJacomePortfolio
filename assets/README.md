# assets

- `logo.png` — extracted from the original inline base64 logo, used for favicon and the two nav/profile marks.
- `contact-photo.jpg` — **missing.** The contact section currently shows a placeholder SVG. Drop the real photo in here as `contact-photo.jpg` (square-ish crop works best, the CSS crops to 1:1) and it will pick it up automatically — no HTML changes needed.
